
============

perf list 权限问题
cat /proc/sys/kernel/perf_event_paranoid
4

sudo sysctl -w kernel.perf_event_paranoid=-1

perf list | grep context-switches
  context-switches OR cs                             [Software event]

sudo sysctl -w kernel.kptr_restrict=0

perf_event_paranoid=-1：允许采样所有内核事件。
kptr_restrict=0：允许看到内核函数的真实地址（符号化展示

============

工具,专项用途,关注点
free -h,宏观状态,内存剩余（Available 而非 Free）
top / ps,进程画像,RES (常驻内存) vs VIRT (虚拟内存)
pmap -x,内存布局,堆、栈、动态库各占用了多少地址空间
valgrind,代码级检测,检测未释放的内存、非法访问
sar -B,内核行为,缺页异常 (pgfault/s) 和扫描率

============
# 所有的 插装函数 列表
sudo bpftrace -l > bpftrace.txt


============

1 perf 的用法
2 numactl 的用法
3 taskset 的用法
参考： https://github.com/miaoZmiao/openEuler-travel.git



============